ollama:
  models:
    - mistral

replicaCount: 1

resources:
  requests:
    cpu: 200m
    memory: 2Gi
  limits:
    cpu: 2000m
    memory: 6Gi

persistentVolume:
  enabled: true
  size: 20Gi

service:
  type: ClusterIP
  port: 11434
